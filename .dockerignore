# 호스트에서 빌드한 바이너리가 이미지로 새어 들어가지 않게 한다.
bin

# 실제 환경변수는 이미지에 굽지 않는다. docker-compose 의 env_file 로 주입한다.
# AuthKey_*.p8 은 Dockerfile 이 COPY 하므로 여기서 제외하면 안 된다.
.env

.git
.gitignore
*.log
playlist.txt
.DS_Store
